# Backend /start endpoint (API Gateway, not a Lambda Function URL).
# POST here returns { "room_url": "...", "status": "ok" } and launches the bot.
VITE_START_URL=https://<your-start-api-id>.execute-api.ap-southeast-2.amazonaws.com/prod
