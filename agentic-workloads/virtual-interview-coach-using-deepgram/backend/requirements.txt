# Minimal app API (Fargate) — session lifecycle + auth glue for G1.

fastapi>=0.110
uvicorn[standard]>=0.29
boto3>=1.34
asyncpg>=0.29
python-jose[cryptography]>=3.3
python-dotenv>=1.0
httpx>=0.27

# F002 (G2) — setup control plane: multipart resume upload + off-gap-clock parsing (R4).
python-multipart>=0.0.9
pypdf>=4.0
python-docx>=1.1

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
black>=24.0
